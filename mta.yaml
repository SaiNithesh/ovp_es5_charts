_schema-version: "3.2"
ID: ovpes5charts
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: ovpes5charts-destination-content
  type: com.sap.application.content
  requires:
  - name: ovpes5charts-destination-service
    parameters:
      content-target: true
  - name: ovpes5charts-repo-host
    parameters:
      service-key:
        name: ovpes5charts-repo-host-key
  - name: ovpes5charts-uaa
    parameters:
      service-key:
        name: ovpes5charts-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: ovpes5charts_html_repo_host
          ServiceInstanceName: ovpes5charts-html5-srv
          ServiceKeyName: ovpes5charts-repo-host-key
          sap.cloud.service: ovpes5charts
        - Authentication: OAuth2UserTokenExchange
          Name: ovpes5charts_uaa
          ServiceInstanceName: ovpes5charts-xsuaa-srv
          ServiceKeyName: ovpes5charts-uaa-key
          sap.cloud.service: ovpes5charts
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: ovpes5charts-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ovpes5charts-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ovpes5charts.zip
      name: ovpes5charts
      target-path: resources/
- name: ovpes5charts
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ovpes5charts-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: ovpes5charts-destination-service
    service-plan: lite
- name: ovpes5charts-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: ovpes5charts-xsuaa-srv
    service-plan: application
- name: ovpes5charts-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ovpes5charts-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
